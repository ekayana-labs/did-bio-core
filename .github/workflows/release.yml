name: release

# Publishes to crates.io through Trusted Publishing when a `v*` tag is
# pushed. No long lived API token: the job exchanges a GitHub OIDC token
# for a short lived crates.io token. Configure the trusted publisher on
# crates.io (repository ekayana-labs/did-bio-core, workflow release.yml,
# environment release) after the first manual publish.

on:
  push:
    tags: ["v*"]

permissions:
  contents: read

jobs:
  publish:
    name: Publish to crates.io
    runs-on: ubuntu-latest
    environment: release
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Tag must match the crate version
        id: crate
        run: |
          name=$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[0].name')
          version=$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[0].version')
          test "v$version" = "$GITHUB_REF_NAME" || { echo "tag $GITHUB_REF_NAME != crate v$version"; exit 1; }
          # The first release is published by hand; skip versions crates.io already has.
          if curl -sf -A "release.yml (github.com/ekayana-labs/did-bio-core)" \
               "https://crates.io/api/v1/crates/$name/$version" -o /dev/null; then
            echo "published=true" >> "$GITHUB_OUTPUT"; echo "$name $version is already on crates.io; nothing to do"
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi
      - run: cargo test --features verify
      - uses: rust-lang/crates-io-auth-action@v1
        if: steps.crate.outputs.published == 'false'
        id: auth
      - run: cargo publish
        if: steps.crate.outputs.published == 'false'
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}
